# AWS-serverless-data-processing-system
Event-driven serverless architecture on AWS that validates customer data via API Gateway to Lambda, data stored in DynamoDB, then stored in oracle lambda and automatically integrates with a third-party. Infrastructure is provisioned using terraform.
